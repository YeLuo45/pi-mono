/**
 * Provider Types
 * V106: Provider Abstraction
 * NOTE: These are declared as 'export const' to work around rolldown [MISSING_EXPORT]
 * bug where interface exports are not visible at runtime bundling.
 */

export const ProviderConfig = 'ProviderConfig';
export const LLMProvider = 'LLMProvider';