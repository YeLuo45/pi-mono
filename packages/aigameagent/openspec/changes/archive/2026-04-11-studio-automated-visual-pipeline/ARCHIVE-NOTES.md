# 归档说明 — studio-automated-visual-pipeline

- 归档日期：2026-04-11
- 内容：图像生成 API、`sharp` 拼 spritesheet、可选 ffmpeg WebM、相关事件类型（见 `apps/studio-server/src/asset-pipeline.ts`）。
- 增量 spec：仍在本目录 `specs/`；主规范 `openspec/specs/` 可在后续评审时合并。
