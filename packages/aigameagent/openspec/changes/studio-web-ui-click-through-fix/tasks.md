## 1. 修复

- [x] 1.1 在 `style.css` 为 `#hud > .row`、`.drawerMask`、`.drawer` 设置合适 `pointer-events`
- [x] 1.2 `npm run build` 通过

## 2. 第二轮

- [x] 2.1 `index.html` 增加 `.hudStack`；`style.css` 中 `.hudStack`、`.log`、`#topRightBar`/`.menuBar` 命中
- [x] 2.2 `OfficeScene`：`isClientOverDomUi` + 平移/滚轮过滤；交互物 `pointerup` 按拖拽距离忽略
- [x] 2.3 `npm run build` 通过
